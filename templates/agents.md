# AGENTS.md

SOT_MODE: sot_files

`AGENTS.md` хранит единственное machine-readable объявление устойчивого режима. Правила находятся в `sops/sot.md`.

Starter не содержит условного поля `SOT_GITHUB_REPOSITORY`. Оно добавляется только в owner-approved checkpoint перехода в `sot_github` и имеет значение в форме `owner/repository`.
