# <Название прохода>

Product Unit: `<путь>`

## Цель

<Какой один проверяемый результат нужно получить.>

## Текущий контур

`ROAD-<id> -> BACK-<id> -> PLAN-<id>-<slug>.md`

Если фактический контур другой, остановись и верни blocker.

## Контекст

1. <Короткий факт, без которого нельзя исполнить проход.>
2. <Ссылка на решение владельца, evidence или другой локальный источник.>

Начальный запрос не заменяет `IE-*`, `OD-*` или `PA-*`. Если действие требует нового решения владельца, не включай это действие в исполняемую часть прохода.

## Разрешено

1. <Файл, каталог или действие.>
2. <Точное исключение защищённой поверхности, если оно одновременно разрешено PLAN и сценарием.>

Список не расширяет `ALLOWED_SURFACES` active PLAN.

## Запрещено

1. <Действие вне границ прохода.>
2. <Действие с owner gate, для которого нет решения.>

Не повторяй весь свод постоянных правил из `AGENTS.md`, `SYSTEM.md` и SOP.

## Шаги

1. Выполнить входной gate.
2. Внести scoped-изменения.
3. Выполнить проверки.
4. Обновить `ROAD -> BACK -> PLAN` и журналы, если это входит в задачу.

## Проверки

```bash
<Команды проверки>
```

<Ручные проверки, включая границу защищённых поверхностей.>

## Исправления дефектов

Перед финальными проверками можно исправить мелкие продуктовые дефекты внутри разрешённой поверхности. Этот блок не расширяет границы задачи. При дефекте Harness в `product-work` выведи `HARNESS_BLOCKER: <краткое описание>` и прекрати изменения; не проектируй исправление и не запрашивай новый режим или PLAN.

## Условия остановки

Верни blocker, если:

1. <Критическое условие.>
2. обнаружен дефект Harness или запрещённое изменение защищённой поверхности.

## Финальный отчёт

Покажи:

1. что выполнено;
2. как изменён `ROAD -> BACK -> PLAN`;
3. какие файлы изменены;
4. какие проверки выполнены;
5. что намеренно не выполнялось;
6. blocker;
7. open item;
8. следующий шаг.
