# Delivery

## Стартовая поставка
- пакет `minesweeper` с игровой логикой и Tkinter UI;
- запуск через `python3 -m minesweeper`;
- unit-тесты игровой логики;
- продуктовый structural check через `Tools/product_check.py`;
- локальный smoke через `Tools/product_bootstrap_smoke.py`.
